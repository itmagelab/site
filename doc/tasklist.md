# План разработки сайта iTMageLAB

## Таблица прогресса

| Итерация | Название | Статус | Дата завершения |
|----------|----------|--------|-----------------|
| 1 | Hero Section (Консольный стиль) | ✅ | 2024-11-04 |
| 2 | About Section (Обо мне) | ✅ | 2024-11-04 |
| 3 | Portfolio Section (Портфолио) | 🔲 | - |
| 4 | Contact Section (Контакты) | 🔲 | - |
| 5 | Footer (Подвал) | 🔲 | - |

**Легенда статусов:**
- 🔲 Не начато
- 🔄 В процессе
- ✅ Завершено

---

## Итерация 1: Hero Section (Консольный стиль)

**Цель:** Создать базовую структуру Yew-приложения с первым блоком - Hero Section в стиле консоли

**Описание:** Полноэкранный блок с заголовком "iTMageLAB" и подписью "Семенов Андрей Владимирович" в стиле консоли с центрированным текстом

### Задачи

- [x] Создать структуру директорий (`src/components/`, `static/`)
- [x] Настроить `index.html` с Tailwind CSS через CDN
- [x] Создать модуль `components.rs` для организации компонентов
- [x] Создать структуру данных `Content` и `HeroContent` с `#[derive(Deserialize)]`
- [x] Реализовать компонент `HeroSection` с асинхронной загрузкой YAML
- [x] Настроить главный компонент `App` в `main.rs`
- [x] Создать `static/content.yaml` с секцией `hero`
- [x] Реализовать console-style дизайн через Tailwind CSS (черный фон, зеленый текст, моноширинный шрифт)

### Технические требования

- Использовать Yew function components
- Данные загружать через `gloo-net::http::Request`
- Обработка ошибок через `Result<T, String>`
- Состояния: Loading, Loaded, Error
- Tailwind CSS utility классы для стилизации
- Полноэкранный блок: `min-h-screen`, `flex`, `items-center`, `justify-center`
- Console стиль: `bg-black`, `text-green-400`, `font-mono`

### Тестирование

- [x] Запустить `trunk build` и проверить компиляцию
- [x] Проверить структуру директорий dist/
- [x] Проверить корректное копирование `static/content.yaml`
- [x] Проверить отсутствие ошибок компиляции
- [x] Готово к запуску `trunk serve` для визуального тестирования

### Ожидаемый результат

Рабочее Yew-приложение с Hero Section:
- Черный фон на весь экран
- Зеленый моноширинный текст
- Заголовок "iTMageLAB"
- Подпись "Семенов Андрей Владимирович"
- Данные загружаются из YAML файла
- Контент центрирован по горизонтали и вертикали

---

## Итерация 2: About Section (Обо мне)

**Цель:** Создать секцию "Обо мне" с фотографией и описанием опыта работы

**Описание:** Двухколоночная раскладка с профессиональным фото слева и текстовым описанием справа

### Задачи

- [x] Создать компонент `AboutSection`
- [x] Добавить структуру данных `AboutContent` с полями: photo, title, description, experience
- [x] Реализовать двухколоночную раскладку (фото + текст)
- [x] Добавить адаптивность для мобильных устройств (вертикальная раскладка)
- [x] Добавить секцию `about` в `static/content.yaml`
- [x] Интегрировать `AboutSection` в главное приложение
- [x] Добавить заглушку для фотографии (иконка Font Awesome)

### Технические требования

- Использовать Yew function components
- Данные загружать из YAML асинхронно
- Обработка ошибок через `Result<T, String>`
- Двухколоночная раскладка на десктопе, вертикальная на мобильных
- Светлый фон для легкости восприятия
- Цветовая схема: мятно-зеленый фон, темно-серый текст
- Адаптивные размеры фото и текста

### Тестирование

- [x] Запустить `trunk build` и проверить компиляцию
- [x] Проверить отображение About секции после Hero
- [x] Проверить корректную загрузку данных из `content.yaml`
- [x] Проверить двухколоночную раскладку на десктопе
- [x] Проверить вертикальную раскладку на мобильных
- [x] Проверить отображение заглушки для фото

### Ожидаемый результат

Секция "Обо мне" с:
- Профессиональным фото (или заглушкой) слева
- Заголовком "Обо мне"
- Описанием опыта работы
- Адаптивной раскладкой
- Данными из YAML файла
- Мятно-зеленым фоном

---

## Архив

После завершения 10 итераций этот файл будет перемещен в `doc/archive/tasklist_001.md`
